# Arquivo de desenvolvimento - copie para .env
DJANGO_SECRET_KEY=dev-secret-key-change-in-production
DJANGO_DEBUG=True

# ALLOWED_HOSTS: Lista de hosts/IPs/domínios que podem acessar o servidor Django
# OBRIGATÓRIO em produção (DEBUG=False). Dj
ango retorna 403 se o host não estiver listado.
# Exemplo produção: DJANGO_ALLOWED_HOSTS=zapi.tambasa.com,40.87.96.136,localhost,127.0.0.1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DJANGO_LOG_LEVEL=DEBUG

# Token do webhook Z-API
ZAPI_WEBHOOK_URL_TOKEN=seu-token-webhook-unico-e-seguro

# Sistema externo para encaminhamento de mensagens
# Suporta múltiplas URLs separadas por vírgula (sistema de fallback automático)
# Exemplo simples: EXTERNAL_SYSTEM_URL=https://seu-sistema.com/api/webhook
# Exemplo com fallback: EXTERNAL_SYSTEM_URL=127.0.0.1:8003,127.0.0.1:8004,192.168.1.100:8003
EXTERNAL_SYSTEM_URL=https://seu-sistema.com/api/webhook
EXTERNAL_SYSTEM_TIMEOUT=10

# Retenção de mensagens (em dias) - mensagens mais antigas serão deletadas automaticamente
MESSAGE_RETENTION_DAYS=5

# Consulta de status de carga - URL do sistema externo
# Suporta múltiplas URLs separadas por vírgula (sistema de fallback automático)
# Exemplo simples: CARGA_STATUS_URL=https://seu-sistema.com/consultastatuscarga/
# Exemplo com fallback: CARGA_STATUS_URL=127.0.0.1:8003,127.0.0.1:8004,192.168.1.100:8003
CARGA_STATUS_URL=https://seu-sistema.com/consultastatuscarga/
CARGA_STATUS_TIMEOUT=10

# Retenção de logs da API (em dias)
API_REQUEST_LOG_RETENTION_DAYS=7

# Webhook de retorno de entrega (delivery callback)
# Token de autenticação para o webhook de entrega
DELIVERY_WEBHOOK_TOKEN=seu-token-delivery-unico-e-seguro

# URL do sistema interno que receberá os callbacks de entrega
INTERNAL_SYSTEM_URL=http://127.0.0.1:8000

# Timeout para requisições ao sistema interno (em segundos)
INTERNAL_FORWARD_TIMEOUT=10

# Retenção de logs do webhook de entrega (em dias)
DELIVERY_WEBHOOK_LOG_RETENTION_DAYS=7


# Configurações de banco (opcional)
# DATABASE_URL=sqlite:///db.sqlite3

# Configurações de banco PostgreSQL
# Para desenvolvimento local:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/webhook_db
# Para produção (exemplo com serviços em nuvem):
# DATABASE_URL=postgresql://usuario:senha@host:5432/webhook_db

# CORS - Origens permitidas para consumir a API via NAVEGADOR WEB (separadas por vírgula)
# ⚠️ IMPORTANTE: CORS é APENAS para navegadores web (JavaScript/AJAX)!
# ⚠️ Requisições de Postman, curl, servidor-para-servidor IGNORAM CORS!
# ⚠️ Se um servidor (40.87.96.136) chama a API, isso NÃO é CORS!
#
# Use CORS_ALLOWED_ORIGINS para listar os domínios de SITES/APPS WEB que farão
# requisições JavaScript/AJAX para sua API.
#
# Formato: http://IP:PORTA ou https://dominio.com
# Exemplos:
#   - https://sistema-externo.com (site que consome a API)
#   - http://192.168.1.100:3000 (app React em desenvolvimento)
#
# Deixe vazio se nenhum navegador web precisar acessar diretamente
CORS_ALLOWED_ORIGINS=https://sistema-externo.com,http://192.168.1.100:3000